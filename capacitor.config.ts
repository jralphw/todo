import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jralphw.todo',
  appName: 'ToDo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
